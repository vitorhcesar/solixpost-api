export enum WalletTransactionTypeEnum {
  CREDIT_PIX = "credit_pix",
  CREDIT_ADMIN = "credit_admin",
  DEBIT = "debit",
}

export enum WalletRechargeStatusEnum {
  PENDING = "pending",
  PAID = "paid",
  FAILED = "failed",
  CANCELED = "canceled",
}
